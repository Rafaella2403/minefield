import { StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    flexDirection: 'row',
  },
  mineFieldContainer: {
    backgroundColor: '#AAA',
    alignItems: 'center',
  },
});
